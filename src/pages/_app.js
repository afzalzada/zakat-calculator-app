// This file is intentionally left empty.
// It is a workaround to satisfy the Next.js build process,
// which sometimes incorrectly looks for a `pages` directory
// even when the App Router (`src/app`) is being used.
